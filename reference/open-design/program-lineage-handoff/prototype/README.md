# 验收原型

`lineage-map-v2.html` 是可点击的验收标准，不是业务源码。

用浏览器直接打开。建议：

1. 先切 **16 对象**，看清主依赖实线和跨支虚线
2. 再切 **520 对象**，确认默认只展开 1 层，点「全部 · 聚类」进入分层总览
3. 对照 `../docs/06-interaction.md` 的验收清单

演示专用、上线前删除：

- 规模切换 16 / 520
- 页脚「交易订单域」
- `localStorage` 键 `lineage-map-v2-tree`
- 前端 `buildGraph()` 假数据
